import React from 'react';

const BrowseGames = () => {
    return (
        <div style={{display:'flex', flexDirection:'column', alignItems:'center'}}>
            <span className="animated_text">Liste des jeux</span>
        </div>
    );
};

export default BrowseGames;
